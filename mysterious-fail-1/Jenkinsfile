#!groovy


pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'cd mysterious-fail-1/ && docker build . -t reversing-figlet'
            }
        }
        stage('Test') {
            steps {
                echo 'TODO - tests!'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
